services:
  - type: web
    name: proper-noun-system
    env: python
    buildCommand: "pip install -r backend/requirements.txt"
    startCommand: "gunicorn -w 4 -k uvicorn.workers.UvicornWorker -b 0.0.0.0:$PORT backend.main:app"
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
